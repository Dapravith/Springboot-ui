org.gradle.jvmargs=-Xmx2g -XX:MaxMetaspaceSize=512m
org.gradle.parallel=true
org.gradle.caching=true

# BootUI is NEVER enabled by a property in this file.
# It is attached to the runtime only when the build is invoked with -Pdev,
# so production artifacts built without that flag contain no BootUI bytecode.
